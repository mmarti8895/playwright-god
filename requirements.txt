# Core dependencies
chromadb>=0.5
click>=8.1

# Optional: LLM backends (install the extras you need)
# openai>=1.0          # pip install "playwright-god[openai]"
# anthropic>=0.25      # pip install "playwright-god[anthropic]"
# google-generativeai>=0.7  # pip install "playwright-god[gemini]"
# requests>=2.28       # pip install "playwright-god[ollama]"
# All at once:         # pip install "playwright-god[all-llms]"

# Development / testing
pytest>=8.0
pytest-cov>=5.0
playwright>=1.40
pytest-playwright>=0.3
